// ReactionPicker component – displays a palette of emojis in a modal\nimport * as React from 'react';\nimport { Dialog, DialogContent, DialogTitle, DialogTrigger } from '@/components/ui/dialog'; // assume dialog component exists\nimport { Emoji } from '@/lib/reactions';\nimport { Button } from './button';\n\ninterface ReactionPickerProps {\n  open: boolean;\n  onClose: () => void;\n  onSelect: (emoji: Emoji) => void;\n}\n\nconst EMOJI_OPTIONS: Emoji[] = [Emoji.THUMBS_UP, Emoji.THUMBS_DOWN, Emoji.LAUGH, Emoji.HEART];\n\nexport const ReactionPicker: React.FC<ReactionPickerProps> = ({ open, onClose, onSelect }) => {\n  return (\n    <Dialog open={open} onOpenChange={(o) => { if (!o) onClose(); }}>\n      <DialogContent className=\"p-4\">\n        <DialogTitle>Choose reaction</DialogTitle>\n        <div className=\"flex space-x-2 mt-2\">\n          {EMOJI_OPTIONS.map((e) => (\n            <Button\n              key={e}\n              size=\"xs\"\n              variant=\"ghost\"\n              onClick={() => { onSelect(e); onClose(); }}\n              aria-label={\`Select ${e}\`}\n            >\n              {e}\n            </Button>\n          ))}\n        </div>\n      </DialogContent>\n    </Dialog>\n  );\n};\n